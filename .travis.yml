language: php

php:
  - 5.4
  - 5.5
  - 5.6
  - 7.0

sudo: false

before_script:
  - composer self-update
  - composer install --prefer-source
  - vendor/bin/phpcs --config-set installed_paths $(pwd)

script:
  - vendor/bin/phpunit

notifications:
  email: false
